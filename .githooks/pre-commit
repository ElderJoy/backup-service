#!/bin/sh
# Match CI: format and clippy before commit. Abort commit if clippy fails.
set -e
cd "$(git rev-parse --show-toplevel)"
cargo fmt --all
git add -u
cargo clippy --workspace --all-targets -- -D warnings
