#!/bin/sh
# Format Rust sources before commit so CI cargo fmt --check passes.
set -e
cd "$(git rev-parse --show-toplevel)"
cargo fmt --all
# Re-stage any files that were reformatted
git add -u
exit 0
